<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test11">

    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/rahul"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/rahul"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".chatidsaver"></activity>
        <activity android:name=".imageclikck" />
        <activity android:name=".notification" />
        <activity android:name=".comment" />
        <activity android:name=".search" />
        <activity android:name=".ViewUpload" />
        <activity android:name=".account" />
        <activity android:name=".homefirst" />
        <activity android:name=".chating" />
        <activity android:name=".ChatPage" />
        <activity android:name=".page1" />
        <activity android:name=".succeslogin" />
        <activity android:name=".info" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".Brodcast"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>